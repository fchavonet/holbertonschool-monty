#include "monty.h"

/**
 * nop - doesn’t do anything :)
 *
 * @stack: a pointer to the top of the stack (not used here).
 * @line_number: the line number being executed (not used here).
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
